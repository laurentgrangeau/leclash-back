# uncomment for init database (first start)
spring.datasource.initialization-mode=always
spring.datasource.schema=classpath*:db/mysql/initDB.sql
spring.datasource.data=classpath*:db/mysql/populateDB.sql

# MySQL config start
#----------------------------------------------------------------
spring.datasource.url=${MYSQL_URL:jdbc:mysql://leclash-db.mysql.database.azure.com/flexibleserverdb}
spring.datasource.username=${MYSQL_USER:panickyTruffle8}
spring.datasource.password=${MYSQL_PASS:SFESwwiEurPJOmbZcIHcWw}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.database=mysql
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=none
#----------------------------------------------------------------
# MySQL config end 
